# 模型配置
MODEL_DIR=models              # 模型目录
DEVICE=cuda                   # 推理设备 (cuda/cpu)
PRELOAD_MODELS=true           # 启动时预加载模型

# OCR 配置
OCR_MODE=local                # OCR 模式 (local/api)
OCR_LANG=ch                   # OCR 语言
USE_ANGLE_CLS=true            # 是否使用角度分类

# 服务配置
HOST=0.0.0.0
PORT=8000
WORKERS=1
DEBUG=true